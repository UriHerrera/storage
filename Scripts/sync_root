#!/bin/bash

source="/home/.nuts/squashfs/"
target="/"

rsync \
    -aHAXr \
    --exclude /proc/ \
    --exclude /sys/ \
    --exclude /dev/ \
    --exclude /run/ \
    --exclude /run/udev/ \
    --exclude /sys/firmware/efi/efivars/ \
    --exclude /etc/shadow \
    --exclude /etc/group \
    --exclude /etc/gshadow \
    --exclude /etc/subgid \
    --exclude /etc/subuid \
    --progress \
    --inplace \
     "$source" "$target"
